import { fileURLToPath } from 'url';
import { dirname, join } from 'path';
import dotenv from 'dotenv';

dotenv.config();

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);
const ROOT = join(__dirname, '..');

export const config = {
  // Chemins
  paths: {
    root: ROOT,
    data: join(ROOT, 'data'),
    templates: join(ROOT, 'templates'),
    output: process.env.OUTPUT_DIR || join(ROOT, 'output'),
    leads: join(ROOT, 'data', 'leads.json'),
    template: join(ROOT, 'templates', 'template.html'),
  },

  // IA
  ai: {
    apiKey: process.env.AI_API_KEY || '',
    model: process.env.AI_MODEL || 'gpt-4o-mini',
    baseUrl: process.env.AI_BASE_URL || 'https://api.openai.com/v1',
    mockMode: process.env.AI_MOCK_MODE === 'true',
    maxTokens: 1500,
    temperature: 0.7,
  },

  // Déploiement
  deploy: {
    mode: process.env.DEPLOY_MODE || 'local', // 'local' | 'vercel'
    vercelToken: process.env.VERCEL_TOKEN || '',
    baseUrl: 'https://votre-domaine.com/demos',
  },

  // Options génération
  generation: {
    concurrency: 3, // traiter N leads en parallèle
    retryAttempts: 2,
    retryDelay: 1000,
  },
};